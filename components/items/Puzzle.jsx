import Card from "../sets/Card";
import Image from "next/image";

export default function Puzzle(props) {
  return (
    <div className="puzzle">
      {/* <Card color={props.color} shadow={props.shadow} subtitleTop={props.title} subtitleBottom={props.text} image={props.image} fundo={true} /> */}

      <style jsx>{``}</style>
    </div>
  );
}
